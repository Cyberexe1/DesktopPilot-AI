# Copy this to .env and fill in values. Never commit .env.
# Cloud backend (AWS App Runner) used by the renderer for /plan and /transcribe.
VITE_CLOUD_API_URL=https://your-app-runner-url.us-east-1.awsapprunner.com
